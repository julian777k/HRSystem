# KeystoneHR 배포 및 운영 가이드 (Windows 환경)

> **작성: 2026-07-04** · 이 문서는 Windows에서 KeystoneHR을 개발·배포·관리하는 방법을 정리합니다.
> 헷갈리면 **"핵심 규칙 1가지"** 만 기억하세요.

---

## 🚨 핵심 규칙 1가지 (제일 중요)

> ### Windows에서 **직접 배포하지 마세요.** 코드를 고치고 `git push`만 하면 자동 배포됩니다.

**이유:** OpenNext(Cloudflare 빌드 도구)는 Windows에서 빌드하면 명령은 성공(exit 0)해도 **실제 사이트가 500 에러로 죽습니다.** 그래서 배포는 항상 GitHub Actions(Linux)에서 자동으로 하도록 만들어 두었습니다.

- ❌ **절대 금지:** `npm run cf:build`, `npm run cf:deploy`, `npx wrangler deploy` 를 내 PC에서 직접 실행
- ✅ **올바른 방법:** 코드 수정 → 커밋 → `git push` → 자동 배포

---

## 1. 일상 개발 (코드 수정할 때)

```bash
cd E:\HR-SYSTEM\hr

# 개발 서버 실행 (로컬 미리보기)
npm run dev
# → http://localhost:3000 에서 확인

# 코드 수정 후 검증 (배포 전 확인, 선택)
npx prisma generate --schema=prisma/schema.sqlite.prisma
npx tsc --noEmit      # 타입 오류 확인
npm test              # 테스트 실행
```

> 💡 타입 체크할 때 `--schema=prisma/schema.sqlite.prisma` 를 꼭 붙이세요.
> 실제 배포는 SQLite(D1) 기준이라, 이걸 안 붙이면 배포와 다른 결과가 나옵니다.

---

## 2. 배포 (사이트에 반영할 때)

```bash
cd E:\HR-SYSTEM

git add .              # 변경한 파일 담기
git commit -m "무엇을 바꿨는지 한글로"
git push               # ← 이것만 하면 자동 배포 시작!
```

`git push` 하는 순간 GitHub Actions가 **자동으로** 아래를 순서대로 실행합니다:

1. **검증** — 타입 체크 + 테스트 (실패하면 배포 안 됨 = 안전장치)
2. **빌드 & 배포** — Linux에서 빌드 → DB 마이그레이션 → 배포
3. **확인** — 사이트가 살아있는지 스모크 테스트

**약 3~4분** 걸립니다.

---

## 3. 배포가 잘 됐는지 확인하는 법

- 웹: **https://github.com/julian777k/HRSystem** → 상단 **Actions** 탭
  - 🟢 초록 체크 = 성공 (사이트 반영됨)
  - 🔴 빨간 X = 실패 (사이트는 이전 상태 그대로 안전 — 클릭해서 원인 확인)

- 명령어로 확인:
```bash
gh run list --workflow=deploy.yml -R julian777k/HRSystem -L 3
```

---

## 4. 문제가 생겼을 때 (긴급 롤백)

배포 후 사이트가 이상하면 **이전 버전으로 즉시 되돌리기**:

```bash
cd E:\HR-SYSTEM\hr

# 배포 이력 확인 (이전 버전 ID 찾기)
npx wrangler deployments list

# 이전 버전으로 롤백 (ID를 위 목록에서 복사)
npx wrangler rollback <이전_버전_ID> --message "롤백 사유"
```

> 롤백은 **코드만** 되돌립니다. DB(D1) 데이터는 그대로 유지됩니다.

---

## 5. 프로덕션 관리 (테넌트·계정 조회 등)

Windows에서 문제없이 가능합니다.

```bash
cd E:\HR-SYSTEM\hr

# 테넌트(고객사) 목록 조회
npx wrangler d1 execute hr-saas-db --remote --command "SELECT subdomain, name, status FROM tenants"

# 관리자 계정 조회
npx wrangler d1 execute hr-saas-db --remote --command "SELECT email, name, role FROM employees WHERE role='SYSTEM_ADMIN'"

# 전체 DB 백업 (정기적으로 하세요!)
npx wrangler d1 export hr-saas-db --remote --output=backup_20260704.sql
```

> 비밀번호 재설정 등은 **슈퍼관리자 콘솔**(https://keystonehr.app/super-admin)에서 하세요.

---

## 6. 참고 정보

| 항목 | 값 |
|------|-----|
| GitHub 저장소 | `julian777k/HRSystem` |
| Cloudflare 계정 ID | `78afd148cc85ba9382f45d51df9bc8ac` |
| D1 데이터베이스 | `hr-saas-db` |
| 프로덕션 도메인 | `keystonehr.app`, `*.keystonehr.app` |
| GitHub Secrets | `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` (등록됨) |

### API 토큰을 바꿔야 할 때
1. Cloudflare 대시보드 → My Profile → API Tokens 에서 재발급
2. GitHub에 등록:
```bash
gh secret set CLOUDFLARE_API_TOKEN --body "새_토큰" -R julian777k/HRSystem
```

---

## ✅ 한 줄 요약

**"Windows에서 코드 고치고 `git push`. 배포·빌드는 GitHub가 알아서. 직접 `cf:deploy` 하지 말 것."**
